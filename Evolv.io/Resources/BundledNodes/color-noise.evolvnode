node "color-noise"(e0, e1) requires(perlin) {
	let v0 = e0 * 50.0
	return float3(
		perlinNoise(coord * v0.x, int(e1.x) + 0),
		perlinNoise(coord * v0.y, int(e1.y) + 1),
		perlinNoise(coord * v0.z, int(e1.z) + 2)
	)
}
